#include<stdio.h>
#include<conio.h>
//Write a C program to transpose of Matrix.
	void main()
{
	int a[3][3],row,col;
	clrscr();
	for(row=0;row<3;row++)
	{
		for(col=0;col<3;col++)
		{
		printf("a[%d][%d]",row,col);
		scanf("%d",&a[row][col]);
		}
	}
	printf("This is Matrix:\n");
	for(row=0;row<3;row++)
	{
		for(col=0;col<3;col++)
		{
		printf("%d\t",a[row][col]);
		}
		printf("\n");
	}
	printf("This is Transpose Matrix:\n");
	for(row=0;row<3;row++)
	{
		for(col=0;col<3;col++)
		{
		printf("%d\t",a[col][row]);
		}
		printf("\n");
		}
		getch();
	}
