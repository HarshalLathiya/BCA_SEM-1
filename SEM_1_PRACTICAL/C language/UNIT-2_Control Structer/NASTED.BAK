#include<stdio.h>
//nasted if else
	void main()
//main carli breaket on
{
	int a,b,c;
	clrscr();
	printf("Eneter First number:");
	scanf("%d",&a);
	printf("Eneter second number:");
	scanf("%d",&b);
	printf("Eneter third number:");
	scanf("%d",&c);

	if(a>=b)
	{
	  if(a>=c)
	  {
	  printf("%d A the largest number\n",a);
	  }
	  else
	  {
	  printf("%d C the equal number\n",c);
	  }
	}
//main else part
	else
	{
	  if(b>=c)
	  {
	  printf("%d B the largest number\n",b);
	  }
	  else
	  {
	  printf("%d C the largest number",c);
	  }
	}
		printf("rest of the program");
		getch();
}